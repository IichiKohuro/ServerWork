using System.Windows.Controls;

namespace ServerHost.UserControls
{
    /// <summary>
    /// Логика взаимодействия для CustomTrayToolTip.xaml
    /// </summary>
    public partial class CustomTrayToolTip : UserControl
    {
        public CustomTrayToolTip()
        {
            InitializeComponent();

            //DataContext = new CustomTrayToolTipViewModel();
        }
    }
}
